import React from 'react';
import { Card } from 'antd';

const Dashboard: React.FC = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      <Card title="Chào mừng bạn đến với XTS Product Traceability">
        <p>Đây là trang tổng quan của hệ thống.</p>
      </Card>
    </div>
  );
};

export default Dashboard;
